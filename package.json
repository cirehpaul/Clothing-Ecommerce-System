{
  "name": "jonel-clothing-ecommerce",
  "version": "1.0.0",
  "private": true,
  "description": "JŌNEL Clothing E-Commerce & CMS Platform",
  "workspaces": [
    "apps/public-web",
    "apps/admin-cms",
    "backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:admin": "npm run dev --workspace=apps/admin-cms",
    "dev:public": "npm run dev --workspace=apps/public-web",
    "build:admin": "npm run build --workspace=apps/admin-cms",
    "build:public": "npm run build --workspace=apps/public-web",
    "build:backend": "npm run build --workspace=backend",
    "db:push": "npm run db:push --workspace=backend",
    "db:seed": "npm run db:seed --workspace=backend",
    "db:generate": "npm run db:generate --workspace=backend"
  }
}
